# Phase 8 Qualification Record — Workload Name

- Date:
- Source commit and working-tree state:
- APK/AAB SHA-256:
- Build variant and signing class:
- Production sound manifest version:
- Device, serial alias, and build fingerprint:
- ADB transport count and selected transport:
- Route and backend:
- Obtained sample rate, channels, burst, buffer, and performance mode:
- Screen state, brightness, timeout, and orientation:
- Media volume and audio focus setup:
- Connectivity and charger state:
- Battery health and starting charge:
- Workload, settings, and duration:
- Exact commands:
- Measurement layer and method:
- Raw-artifact location and checksums:

## Results

| Budget | Population | Result | Pass/fail/gap |
| --- | --- | --- | --- |
| TB-___ |  |  |  |

Record intended, rendered, written, and correlated presented frames; duplicate frames; catch-up events; mixed configurations; deadline misses; dropped events; underruns and skipped frames; render CPU; PSS/RSS; thermal state; and battery change where applicable.

For acoustic work, record detected, expected, missed, doubled, and manually reviewed events; fitted drift; p50/p95/p99/max absolute inter-onset error; threshold sweep; recorder clock; microphone/interface; placement; and route.

## Limitations and exclusions

State unavailable instrumentation, unsupported routes, environmental changes, collector interference, invalid runs, and claims that remain intentionally unmade. Never generalize Pixel 8a speaker results to other Android devices or Bluetooth.

## Device restoration

Record the restored brightness, volume, timeout, orientation, connectivity, charger, keep-awake, and other settings. If nothing changed, state that explicitly.
