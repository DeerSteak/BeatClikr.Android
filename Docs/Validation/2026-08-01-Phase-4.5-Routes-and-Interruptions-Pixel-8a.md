# Phase 4.5 routes and interruptions — 2026-08-01

The source, device, build variant, volume, and APK identity match the Phase 4.4 record. Only the built-in speaker was available. Wired, USB audio, and Bluetooth hardware were unavailable and are not marked tested.

The release-equivalent `AudioEngineInstrumentedTest` and `AudioStartupLatencyInstrumentedTest` suite passed all four tests on the built-in route. While BeatClikr was playing, `dumpsys audio` reported it as the focus owner and the UI showed Pause. Sending `KEYCODE_SLEEP` caused the process-lifecycle teardown after settling: the focus stack became empty, the session stopped, and waking/unlocking showed Play without automatic resume. Device brightness 12, automatic-brightness mode 1, ten-minute timeout, airplane mode off, and `stay_on_while_plugged_in=0` were recorded; the temporary stay-awake value used during testing was restored to 0.

Calls, noisy-device removal, route connect/disconnect, wired, USB, and Bluetooth observations were unavailable. `audioserver` restart was not attempted and is not claimed. Media-server loss, route removal/change, and focus-loss variants have synthesized JVM/integration coverage in `PlaybackCoordinatorTest`, `ActiveOutputRouteTrackerTest`, and `PlaybackRouteWiringTest`; those tests are not physical observations. The later accepted findings are consolidated in `benchmarks/2026-07-30-phase-3-and-4-summary.md` and the Phase 8 qualification summary.

## Product-owner completion

On 2026-08-02, the product owner reported completing the remaining route and interruption device scenarios and accepted the route/interruption matrix as solid and complete. This closes the manual acceptance item. No additional commands, measurements, or raw artifacts were supplied, so the detailed observations above remain the independently recorded evidence rather than being retroactively expanded.
